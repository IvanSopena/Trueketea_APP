<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary
    x:Class="TrueketeaApp.Styles.EntryStyles"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:control="clr-namespace:TrueketeaApp.MyControls" >

    <!-- Common style for borderless entries -->
    <Style x:Key="BorderlessEntryStyle" TargetType="control:BorderlessEntry">
        <Setter Property="FontFamily" Value="RalewayRegular" />
        <Setter Property="PlaceholderColor" Value="{DynamicResource Gray-700}" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="TextColor" Value="{DynamicResource Gray-900}" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="VerticalOptions" Value="Center" />
        <Setter Property="ReturnType" Value="Done" />
    </Style>

    <Style x:Key="BorderlessEditorStyle" TargetType="control:BorderlessEditor">
        <Setter Property="TextColor" Value="{DynamicResource Gray-900}" />
        <Setter Property="HorizontalOptions" Value="FillAndExpand" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="PlaceholderColor" Value="{DynamicResource Gray-700}" />
    </Style>
    
</ResourceDictionary>